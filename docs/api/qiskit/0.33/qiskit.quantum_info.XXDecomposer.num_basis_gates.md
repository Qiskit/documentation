# qiskit.quantum\_info.XXDecomposer.num\_basis\_gates

`XXDecomposer.num_basis_gates(unitary)`

Counts the number of gates that would be emitted during re-synthesis.

NOTE: Used by ConsolidateBlocks.
