# qiskit.pulse.transforms.AlignFunc.to\_dict

`AlignFunc.to_dict()`

Returns dictionary to represent this alignment.

<Admonition title="Note" type="note">
  `func` is not presented in this dictionary. Just name.
</Admonition>

**Return type**

`Dict`\[`str`, `Any`]
