---
title: Use IBM Cloud Resource Controller API for instance management
description: Parameters to set when provisioning a Qiskit Runtime service instance, and extension fields returned when accessing an instance.

---

# Use IBM Cloud Resource Controller API for instance management

You can use the IBM Cloud&reg; Resource Controller REST API to programmatically get, create, and update [instances](/docs/guides/instances). 

All Resource Controller endpoints require that you authenticate by passing a header called `Authorization` with the bearer token. Refer to /docs/guides/cloud-setup-rest-api.

TODO: inline. When creating and updating instances, you should set `parameters` in the body, as explained in the [following section](#parameters). When getting instances, you should read from `extensions` rather than `parameters`, as explained in the [following section](#extensions).

## Get an instance

[Get an instance](https://cloud.ibm.com/apidocs/resource-controller/resource-controller#get-resource-instance) 

- URL encoding the CRN
- explain extensions. Use extensions not parameters

## Get a list of all instances

[Get a list of all instances](https://cloud.ibm.com/apidocs/resource-controller/resource-controller#list-resource-instances)  

    ```
        params: dict[str, Any] = {
            "resource_id": b6049020-80f4-11eb-a0f7-e35ec9b4054f,
            "account_id": account_id,
            "resource_plan_id": plan_id_for(plan),  # TODO: get all the values
        }
    ```

Similar to when [getting a single instance](#get-an-instance), the results will include both `extensions` and `parameters`. Use `extensions` to 

## Update an instance

[Update an instance](https://cloud.ibm.com/apidocs/resource-controller/resource-controller#update-resource-instance)

TODO: Set `parameters`. Partial object is okay.

To integrate with Qiskit Functions, refer to [here](#integrate-with-qiskit-functions).

## Create a new instance

[Create (provision) a new instance](https://cloud.ibm.com/apidocs/resource-controller/resource-controller#create-resource-instance)

To integrate with Qiskit Functions, refer to [here](#integrate-with-qiskit-functions).

## Integrate with Qiskit Functions

