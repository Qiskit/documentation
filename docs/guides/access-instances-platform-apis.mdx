---
title: Use IBM Cloud Resource Controller API for instance management
description: Parameters to set when provisioning a Qiskit Runtime service instance, and extension fields returned when accessing an instance.

---

# Use IBM Cloud Resource Controller API for instance management

You can use the IBM Cloud&reg; Resource Controller REST API to programmatically get, create, and update [instances](/docs/guides/instances). 

All Resource Controller endpoints require that you authenticate by passing a header called `Authorization` with the bearer token. Refer to /docs/guides/cloud-setup-rest-api.

## Get an instance

Use the [`GET /v2/resource_instances/{crn}`](https://cloud.ibm.com/apidocs/resource-controller/resource-controller#list-resource-instances) endpoint to get information about a particular instance. You must URL encode the CRN's instance in the path, such as with TODO curl command. 

In addition to the standard Resource Controller fields, the result will add Quantum-specific information to `parameters` and `extensions`. You should always use `extensions` instead of `parameters`. `extensions` includes these fields:

- `instance_limit_seconds` (integer or `null`, >= 0): The time limit to set on the instance.
- `usage_allocation_seconds` (integer or `null`, >= 0): Allotted time for the instance, used by the fair-share scheduler to determine the priority in the queue, based on usage of all QPUs.
- `backends` (Required; array of lists of backends (strings) or array with "ANY" (strings)): The allow-list of names of backends that are available to this instance. Default: `["ANY"]`, means all backends are available on this plan. `[]` means no backends.

## Get a list of all instances

Use the [`GET /v2/resource_instances`](https://cloud.ibm.com/apidocs/resource-controller/resource-controller#list-resource-instances) endpoint to get a list of all your instances. 

- Set resource_id query param to b6049020-80f4-11eb-a0f7-e35ec9b4054f to filter to quantum
- Optionally, set resource_plan_id query param if you have multiple plans in your account and want to filter by plan
    "premium": "7f666d17-7893-47d8-bf9d-2b2389fc4dfc",
    "flex": "53bde9d3-cdbb-46f5-a98f-60ebcadf7260",
    "pay-go": "5304b575-3cff-4455-90dc-ae4367762093",
    "open": "850b21a7-71de-4e53-9441-1abdd202f35d",

Similar to when [getting a single instance](#get-an-instance), the results will include both `extensions` and `parameters`. Use `extensions` rather than `parameters`. 

## Update an instance

[Update an instance](https://cloud.ibm.com/apidocs/resource-controller/resource-controller#update-resource-instance)

TODO: Set `parameters`. Partial object is okay.

## Create a new instance

[Create (provision) a new instance](https://cloud.ibm.com/apidocs/resource-controller/resource-controller#create-resource-instance)
